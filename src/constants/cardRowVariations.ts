import { CardRowVariation } from "@/types/cards";

const cardRowVariations: {
  DETAIL: CardRowVariation
  TOTAL: CardRowVariation
} = {
  DETAIL: "detail",
  TOTAL: "total",
};

export default cardRowVariations;
